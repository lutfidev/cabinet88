<?xml version="1.0" encoding="utf-8"?>
<!--
  The window behind the app while the process starts. It is the app's own
  background, not white and not the system default, so launching Cabinet88 does
  not flash a bright frame before the dark cabinet arrives.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background" />
</layer-list>
